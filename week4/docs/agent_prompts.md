# 1. Generate Tests Agent Command
"Act as a QA Engineer. Scan the `backend/app/routers/` directory. Find any endpoints that don't have tests in `backend/tests/`. Generate the missing pytest functions using FastAPI TestClient. Do not modify existing tests."

# 2. Improve Docs Agent Command
"Act as a Documentation Expert. Scan the `backend/app/` directory. Find functions and classes missing docstrings. Add Python standard docstrings to them explaining parameters and return types."